---
phase: 04-spatial-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/map/BBoxDrawing.tsx, src/hooks/useMapMode.ts, src/components/map/ModeBadge.tsx, src/components/map/MapView.tsx]
autonomous: true
requirements: [FLT-02, FLT-05]
user_setup: []
must_haves:
  truths:
    - "User can draw a bounding box on map to show only parcels within"
    - "Drawing rectangle appears blue with 10% opacity fill"
    - "URL updates with ?bbox parameter when drawing completes"
    - "Clear filters button or exit mode clears bbox"
  artifacts:
    - path: "src/components/map/BBoxDrawing.tsx"
      provides: "Bounding box drawing handler with rectangle overlay"
      min_lines: 70
      exports: ["BBoxDrawing"]
    - path: "src/hooks/useMapMode.ts"
      provides: "Map mode state machine hook"
      min_lines: 60
      exports: ["useMapMode"]
    - path: "src/components/map/ModeBadge.tsx"
      provides: "Floating mode indicator badge"
      min_lines: 50
      exports: ["ModeBadge"]
  key_links:
    - from: "src/components/map/BBoxDrawing.tsx"
      to: "react-leaflet"
      via: "useMapEvents, Rectangle components"
      pattern: "useMapEvents\\(\\{ .*click.*mousemove.*\\}\\)"
    - from: "src/hooks/useMapMode.ts"
      to: "window"
      via: "Escape key event listener"
      pattern: "addEventListener\\('keydown'.*Escape"
    - from: "src/components/map/MapView.tsx"
      to: "src/components/map/BBoxDrawing.tsx"
      via: "Conditional render when mode is bbox"
      pattern: "mode === 'bbox'"
---

<objective>
Implement bounding box drawing mode for spatial filtering. Users can draw a rectangle on the map to filter parcels within that area, with the bbox state synchronized to URL parameters.

Purpose: Enable spatial filtering by geographic area, complementing status filtering with location-based selection.

Output: BBoxDrawing component, useMapMode hook, ModeBadge indicator, and MapView integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spatial-analysis/04-RESEARCH.md
@.planning/phases/04-spatial-analysis/04-UI-SPEC.md
@.planning/phases/03-crud-operations/03-03-SUMMARY.md (DrawingHandler pattern)
@src/components/map/DrawingHandler.tsx (for useMapEvents pattern)
@src/lib/url-utils.ts (for parseBbox, buildUrlParams)
@src/hooks/useFilterParams.ts (for setFilters integration)
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/lib/url-utils.ts (created in 04-01):
```typescript
export function parseBbox(bboxParam: string | null): L.LatLngBounds | null
export function buildUrlParams(filters: FilterState): URLSearchParams
```

From src/hooks/useFilterParams.ts (created in 04-01):
```typescript
interface FilterState {
  status: ParcelStatus[]
  bbox: L.LatLngBounds | null
  selected: number | null
}
function useFilterParams(): { filters: FilterState, setFilters: (f: FilterState) => void, clearFilters: () => void }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useMapMode hook for mode state machine</name>
  <files>src/hooks/useMapMode.ts</files>
  <read_first>
    - src/hooks/useFilterParams.ts (for React hooks pattern)
    - .planning/phases/04-spatial-analysis/04-RESEARCH.md (Pattern 2: Map Mode State Machine)
  </read_first>
  <behavior>
    - Hook returns { mode, modeData, enterBboxMode, enterBufferMode, exitMode }
    - mode starts as 'normal'
    - enterBboxMode sets mode to 'bbox'
    - enterBufferMode sets mode to 'buffer-point'
    - exitMode sets mode to 'normal' and clears modeData
    - Escape key calls exitMode
    - Cleanup removes event listener
  </behavior>
  <action>
    Create src/hooks/useMapMode.ts with the following:

    1. Import useState, useEffect, useCallback from 'react'
    2. Import L from 'leaflet' for LatLngBounds type

    Define types:
    ```typescript
    export type MapMode = 'normal' | 'bbox' | 'buffer-point'

    export interface ModeData {
      bbox?: L.LatLngBounds
      bufferCenter?: L.LatLng
      bufferRadius?: number
    }
    ```

    Hook implementation:
    - Initialize mode state: useState<MapMode>('normal')
    - Initialize modeData state: useState<ModeData>({})
    - enterBboxMode callback: setMode('bbox')
    - enterBufferMode callback: setMode('buffer-point')
    - exitMode callback: setMode('normal'), setModeData({})

    - Escape key handler with useEffect:
      * Add 'keydown' listener to window
      * Check if e.key === 'Escape'
      * Call exitMode
      * Cleanup: removeEventListener

    Return { mode, modeData, setModeData, enterBboxMode, enterBufferMode, exitMode }
  </action>
  <verify>
    <automated>npm test -- --run useMapMode.test.tsx</automated>
  </verify>
  <done>
    - File src/hooks/useMapMode.ts exists
    - Exports useMapMode hook and MapMode type
    - Hook manages mode state (normal/bbox/buffer-point)
    - Escape key exits current mode
    - modeData stores bbox, bufferCenter, bufferRadius
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ModeBadge component for mode indicator</name>
  <files>src/components/map/ModeBadge.tsx</files>
  <read_first>
    - src/components/map/MapStatusBar.tsx (for existing badge component pattern)
    - .planning/phases/04-spatial-analysis/04-UI-SPEC.md (Mode Badge section)
  </read_first>
  <behavior>
    - Renders mode badge with icon and text
    - Shows "Drawing Box" with BoxSelect icon for bbox mode
    - Shows "Select Center" with Target icon for buffer mode
    - Hidden when mode is 'normal'
    - Exit button (X) calls onExit
  </behavior>
  <action>
    Create src/components/map/ModeBadge.tsx with the following:

    Props interface:
    ```typescript
    interface ModeBadgeProps {
      mode: MapMode
      onExit: () => void
    }
    ```

    Component structure:
    - Return null when mode === 'normal'
    - Container: absolute top-4 left-4 z-15 (below header, above map)
    - Badge div: "flex items-center gap-2 px-3 py-2 bg-white shadow-md rounded-full"
    - Icon based on mode:
      * bbox: BoxSelect from lucide-react, text-blue-500
      * buffer-point: Target from lucide-react, text-blue-500
    - Text based on mode:
      * bbox: "Drawing Box"
      * buffer-point: "Select Center"
    - Exit button: X icon from lucide-react, hover:text-slate-700
    - Text classes: "text-sm font-medium text-slate-700"

    Import MapMode type from '@/hooks/useMapMode'
    Import icons from lucide-react
  </action>
  <verify>
    <automated>npm test -- --run ModeBadge.test.tsx</automated>
  </verify>
  <done>
    - File src/components/map/ModeBadge.tsx exists
    - Component shows correct icon and text per mode
    - Hidden when mode is 'normal'
    - Exit button calls onExit callback
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create BBoxDrawing component with rectangle overlay</name>
  <files>src/components/map/BBoxDrawing.tsx</files>
  <read_first>
    - src/components/map/DrawingHandler.tsx (for useMapEvents pattern)
    - .planning/phases/04-spatial-analysis/04-RESEARCH.md (Pattern 4: Leaflet Spatial Overlays)
  </read_first>
  <behavior>
    - Renders Rectangle while dragging
    - First click sets startPoint
    - Mousemove updates currentPoint
    - Second click completes bbox and calls onComplete
    - Escape key calls onCancel
    - Rectangle has blue-500 color, 10% fill opacity
  </behavior>
  <action>
    Create src/components/map/BBoxDrawing.tsx with the following:

    Props interface:
    ```typescript
    interface BBoxDrawingProps {
      onComplete: (bounds: L.LatLngBounds) => void
      onCancel: () => void
    }
    ```

    Component implementation:
    - Import { useMapEvents, Rectangle } from 'react-leaflet'
    - Import L from 'leaflet'
    - useState for startPoint and currentPoint (both L.LatLng | null)

    - useMapEvents handlers:
      * click: If no startPoint, set it. If startPoint exists, create bounds and call onComplete(L.latLngBounds(startPoint, e.latlng)), then reset points
      * mousemove: If startPoint, setCurrentPoint(e.latlng)
      * keydown (Escape): Call onCancel, reset points

    - Rectangle rendering (only when startPoint and currentPoint exist):
      * bounds: L.latLngBounds(startPoint, currentPoint)
      * pathOptions: color '#3b82f6', fillColor '#3b82f6', fillOpacity 0.1, weight 2

    - Return null (component only renders Rectangle conditionally)

    NOTE: Use L.DomEvent.stopPropagation on click to prevent parcel click during drawing.
  </action>
  <verify>
    <automated>npm test -- --run BBoxDrawing.test.tsx</automated>
  </verify>
  <done>
    - File src/components/map/BBoxDrawing.tsx exists
    - Component renders blue rectangle while dragging
    - Click-drag-click completes bbox
    - Escape key cancels drawing
    - onComplete callback receives LatLngBounds
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate bbox mode into MapView and App</name>
  <files>src/components/map/MapView.tsx, src/App.tsx</files>
  <read_first>
    - src/components/map/MapView.tsx (current structure)
    - src/App.tsx (current state management)
    - src/hooks/useMapMode.ts (mode hook)
    - src/hooks/useFilterParams.ts (filter hook)
  </read_first>
  <action>
    Update MapView.tsx:
    1. Add new props: mode?: MapMode, onBboxComplete?: (bounds: L.LatLngBounds) => void, onExitMode?: () => void
    2. Add ModeBadge component when mode is not 'normal'
    3. Add BBoxDrawing component when mode === 'bbox' and callbacks provided
    4. Update cursor to 'crosshair' when mode === 'bbox'

    Update App.tsx:
    1. Import useMapMode from '@/hooks/useMapMode'
    2. Call hook: const { mode, exitMode } = useMapMode()
    3. Add handleBboxComplete function:
       ```typescript
       const handleBboxComplete = (bounds: L.LatLngBounds) => {
         setFilters({ ...filters, bbox: bounds })
         exitMode()
       }
       ```
    4. Pass mode and onBboxComplete to MapView
    5. Add "Draw Box" button to header (MapHeader) that calls enterBboxMode
    6. For now, add enterBboxMode to useMapMode hook call: const { mode, enterBboxMode, exitMode } = useMapMode()
    7. Wire up button in header (temporary until Plan 03)

    Update MapHeader.tsx:
    1. Add onDrawBox?: () => void prop
    2. Add "Draw Box" button with BoxSelect icon
    3. Button styling: bg-slate-100 text-slate-700 hover:bg-slate-200 when inactive
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - MapView accepts mode prop
    - ModeBadge shows when drawing bbox
    - BBoxDrawing renders rectangle when mode is bbox
    - Completing bbox updates filters.bbox and exits mode
    - URL updates with ?bbox parameter
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→map | Mouse events create spatial data (bbox coordinates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T (Tampering) | bbox coordinates | accept | Coordinates captured from Leaflet (trusted source), validated by Leaflet LatLngBounds |
| T-04-02-02 | I (Injection) | buildUrlParams with bbox | accept | URLSearchParams API auto-encodes; coords are numeric from Leaflet |
</threat_model>

<verification>
1. Click "Draw Box" button in header
2. Draw rectangle on map (click-drag-click)
3. Verify blue rectangle appears while drawing
4. Verify URL updates with ?bbox parameter
5. Verify ModeBadge shows "Drawing Box"
6. Press Escape to cancel or complete bbox
7. Run `npm test -- --run BBoxDrawing.test.tsx useMapMode.test.tsx ModeBadge.test.tsx`
8. Run `npm run build`
</verification>

<success_criteria>
- User can draw bounding box on map
- Blue rectangle appears during drawing
- URL updates with ?bbox=minLng,minLat,maxLng,maxLat
- ModeBadge shows "Drawing Box" during mode
- Escape key exits mode without saving
- Clear filters clears bbox
</success_criteria>

<output>
After completion, create `.planning/phases/04-spatial-analysis/04-02-SUMMARY.md`
</output>
