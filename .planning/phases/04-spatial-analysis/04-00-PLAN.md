---
phase: 04-spatial-analysis
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test stub files exist for all Phase 4 components and hooks"
    - "All test files compile without TypeScript errors"
    - "Test suite can run with npm test command"
  artifacts:
    - path: "src/components/map/__tests__/FilterBar.test.tsx"
      provides: "Test stub for FilterBar component"
      min_lines: 10
    - path: "src/components/map/__tests__/BufferPanel.test.tsx"
      provides: "Test stub for BufferPanel component"
      min_lines: 10
    - path: "src/components/map/__tests__/BufferVisualization.test.tsx"
      provides: "Test stub for BufferVisualization component"
      min_lines: 10
    - path: "src/components/map/__tests__/ModeBadge.test.tsx"
      provides: "Test stub for ModeBadge component"
      min_lines: 10
    - path: "src/components/map/__tests__/BBoxDrawing.test.tsx"
      provides: "Test stub for BBoxDrawing component"
      min_lines: 10
    - path: "src/hooks/__tests__/useFilterParams.test.ts"
      provides: "Test stub for useFilterParams hook"
      min_lines: 10
    - path: "src/hooks/__tests__/useBufferAnalysis.test.ts"
      provides: "Test stub for useBufferAnalysis hook"
      min_lines: 10
    - path: "src/hooks/__tests__/useMapMode.test.ts"
      provides: "Test stub for useMapMode hook"
      min_lines: 10
    - path: "src/lib/__tests__/url-utils.test.ts"
      provides: "Test stub for url-utils functions"
      min_lines: 10
  key_links: []
---

<objective>
Create test stub files for all Phase 4 components and hooks to enable test-driven development and continuous verification during implementation.

Purpose: Test stubs provide Nyquist-compliant verification targets for subsequent plans. Each stub defines the test structure that will be filled in as components are implemented.

Output: 9 test stub files that compile and run with placeholder tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spatial-analysis/04-VALIDATION.md
@.planning/phases/04-spatial-analysis/04-RESEARCH.md
@src/test/setup.ts
@src/test/map-test-utils.tsx
@src/components/map/__tests__/ParcelLayer.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterBar test stub</name>
  <files>src/components/map/__tests__/FilterBar.test.tsx</files>
  <read_first>
    - src/test/map-test-utils.tsx (for renderWithMapContext pattern)
    - src/components/map/__tests__/MapHeader.test.tsx (for existing component test pattern)
  </read_first>
  <action>
    Create test stub file for FilterBar component with placeholder tests for:
    - Rendering status filter buttons (Free, Negotiating, Target)
    - Toggling status filters on button click
    - Showing Clear Filters button when filters are active
    - Clearing all filters on Clear button click

    Use the same pattern as MapHeader.test.tsx:
    - Import describe, it, expect, vi from vitest
    - Use renderWithMapContext for rendering
    - Mock lucide-react icons (Filter, X) if needed
    - Add placeholder tests with expect(true).toBe(true) for now
  </action>
  <verify>
    <automated>npm test -- --run FilterBar.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/components/map/__tests__/FilterBar.test.tsx
    - File contains at least 4 test cases (render, toggle, clear button shown, clear action)
    - Test runs without errors (placeholder tests pass)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BufferPanel test stub</name>
  <files>src/components/map/__tests__/BufferPanel.test.tsx</files>
  <read_first>
    - src/test/map-test-utils.tsx
    - src/components/map/__tests__/DrawingToolbar.test.tsx (for existing control component pattern)
  </read_first>
  <action>
    Create test stub file for BufferPanel component with placeholder tests for:
    - Rendering distance input field with default value (500)
    - Showing Apply and Cancel buttons
    - Validating distance input (1-10000 range)
    - Calling onApply with distance when Apply clicked
    - Calling onCancel when Cancel clicked

    Use the same pattern as DrawingToolbar.test.tsx:
    - Mock userEvent for input interactions
    - Test distance validation with zod schema
    - Add placeholder tests with expect(true).toBe(true) for now
  </action>
  <verify>
    <automated>npm test -- --run BufferPanel.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/components/map/__tests__/BufferPanel.test.tsx
    - File contains at least 5 test cases (render, buttons, validation, apply, cancel)
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create BufferVisualization test stub</name>
  <files>src/components/map/__tests__/BufferVisualization.test.tsx</files>
  <read_first>
    - src/test/map-test-utils.tsx
    - src/components/map/__tests__/ParcelLayer.test.tsx (for GeoJSON component test pattern)
  </read_first>
  <action>
    Create test stub file for BufferVisualization component with placeholder tests for:
    - Rendering buffer circle with correct center and radius
    - Rendering center point marker
    - Highlighting nearby parcels in blue
    - Fading non-matching parcels

    Mock react-leaflet components (Circle, CircleMarker):
    ```tsx
    vi.mock('react-leaflet', () => ({
      Circle: ({ center, radius }: any) => (
        <div data-testid="buffer-circle" data-radius={radius} />
      ),
      CircleMarker: ({ center }: any) => (
        <div data-testid="center-marker" />
      ),
    }))
    ```
  </action>
  <verify>
    <automated>npm test -- --run BufferVisualization.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/components/map/__tests__/BufferVisualization.test.tsx
    - File contains at least 4 test cases
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Create ModeBadge test stub</name>
  <files>src/components/map/__tests__/ModeBadge.test.tsx</files>
  <read_first>
    - src/test/map-test-utils.tsx
    - src/components/map/__tests__/MapStatusBar.test.tsx (for existing badge/label component pattern)
  </read_first>
  <action>
    Create test stub file for ModeBadge component with placeholder tests for:
    - Rendering mode name with icon
    - Showing correct text for each mode (Drawing Box, Select Center)
    - Rendering exit mode button (X icon)
    - Calling onExit when exit button clicked
    - Hiding when mode is 'normal'

    Mock lucide-react icons (BoxSelect, Target, X) as needed.
  </action>
  <verify>
    <automated>npm test -- --run ModeBadge.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/components/map/__tests__/ModeBadge.test.tsx
    - File contains at least 5 test cases
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 5: Create BBoxDrawing test stub</name>
  <files>src/components/map/__tests__/BBoxDrawing.test.tsx</files>
  <read_first>
    - src/test/map-test-utils.tsx
    - src/components/map/__tests__/DrawingHandler.test.tsx (for map drawing component pattern)
  </read_first>
  <action>
    Create test stub file for BBoxDrawing component with placeholder tests for:
    - Rendering rectangle while dragging
    - Completing bbox on mouse release
    - Calling onComplete with LatLngBounds
    - Clearing drawing on cancel

    Mock react-leaflet components (Rectangle, useMapEvents) following DrawingHandler pattern.
  </action>
  <verify>
    <automated>npm test -- --run BBoxDrawing.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/components/map/__tests__/BBoxDrawing.test.tsx
    - File contains at least 4 test cases
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 6: Create useFilterParams hook test stub</name>
  <files>src/hooks/__tests__/useFilterParams.test.ts</files>
  <read_first>
    - src/test/map-test-utils.tsx (for createWrapper pattern)
    - src/hooks/__tests__/useCreateParcel.test.tsx (for React Query hook test pattern)
  </read_first>
  <action>
    Create test stub file for useFilterParams hook with placeholder tests for:
    - Parsing status from URL params on mount
    - Parsing bbox from URL params on mount
    - Parsing selected parcel ID from URL params
    - Updating URL when filters change
    - Clearing all filters resets URL

    Test file must be .tsx (not .ts) to support toast imports.
    Use renderHook from @testing-library/react for testing custom hooks.
  </action>
  <verify>
    <automated>npm test -- --run useFilterParams.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/hooks/__tests__/useFilterParams.test.tsx
    - File contains at least 5 test cases
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 7: Create useBufferAnalysis hook test stub</name>
  <files>src/hooks/__tests__/useBufferAnalysis.test.ts</files>
  <read_first>
    - src/test/map-test-utils.tsx
    - src/hooks/__tests__/useCreateParcel.test.tsx
    - src/api/types.ts (for BufferResult type)
  </read_first>
  <action>
    Create test stub file for useBufferAnalysis hook with placeholder tests for:
    - Fetching buffer results from API
    - Passing center and radius to API
    - Validating request with bufferRequestSchema
    - Enabling query only when center is set
    - Handling loading and error states

    Test file must be .tsx (not .ts) to support toast imports.
    Mock api.post from '@/api/axios'.
  </action>
  <verify>
    <automated>npm test -- --run useBufferAnalysis.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/hooks/__tests__/useBufferAnalysis.test.tsx
    - File contains at least 5 test cases
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 8: Create useMapMode hook test stub</name>
  <files>src/hooks/__tests__/useMapMode.test.ts</files>
  <read_first>
    - src/test/map-test-utils.tsx
    - src/hooks/__tests__/useCreateParcel.test.tsx
  </read_first>
  <action>
    Create test stub file for useMapMode hook with placeholder tests for:
    - Starting in 'normal' mode
    - Entering bbox mode with enterBboxMode
    - Entering buffer mode with enterBufferMode
    - Exiting mode with exitMode
    - Exiting mode on Escape key press
    - Clearing modeData on mode exit

    Test file must be .tsx (not .ts) to support toast imports.
    Test Escape key handling by dispatching keyboard event.
  </action>
  <verify>
    <automated>npm test -- --run useMapMode.test.tsx</automated>
  </verify>
  <done>
    - File exists at src/hooks/__tests__/useMapMode.test.tsx
    - File contains at least 6 test cases
    - Test runs without errors
  </done>
</task>

<task type="auto">
  <name>Task 9: Create url-utils test stub</name>
  <files>src/lib/__tests__/url-utils.test.ts</files>
  <read_first>
    - src/lib/__tests__/utils.test.ts (for existing utility test pattern)
  </read_first>
  <action>
    Create test stub file for url-utils functions with placeholder tests for:
    - parseStatus('free,negotiating') returns ['free', 'negotiating']
    - parseStatus(null) returns []
    - parseStatus('invalid') returns [] (defensive)
    - parseBbox('106.8,-6.2,106.9,-6.1') returns valid LatLngBounds
    - parseBbox(null) returns null
    - parseBbox('invalid') returns null (defensive)
    - buildUrlParams creates URLSearchParams from filter state
    - buildUrlParams with empty filters returns empty URLSearchParams

    Import functions from '@/lib/url-utils' once created.
    Mock Leaflet L.latLngBounds for bbox tests.
  </action>
  <verify>
    <automated>npm test -- --run url-utils.test.ts</automated>
  </verify>
  <done>
    - File exists at src/lib/__tests__/url-utils.test.ts
    - File contains at least 8 test cases
    - Test runs without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test stubs have no security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | N/A | Test stubs | accept | No security surface in test code |
</threat_model>

<verification>
1. Run `npm test -- --run` to verify all test stubs pass
2. Check TypeScript compilation with `npx tsc -b` (ignoring test files for now)
3. Verify all 9 test files exist in correct directories
</verification>

<success_criteria>
- All 9 test stub files created
- Test suite runs without errors
- Each stub has placeholder tests for documented behaviors
- nyquist_compliant set to true in 04-VALIDATION.md frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/04-spatial-analysis/04-00-SUMMARY.md`
</output>
