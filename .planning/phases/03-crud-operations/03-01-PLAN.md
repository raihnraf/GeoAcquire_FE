---
phase: 03-crud-operations
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - src/lib/zod.ts
  - src/api/axios.ts
  - src/api/types.ts
autonomous: true
requirements: [CRUD-06, CRUD-07]
user_setup: []

must_haves:
  truths:
    - "Zod parcelSchema validates all form fields with proper constraints"
    - "Axios interceptor converts Laravel 422 error format to react-hook-form format"
    - "API types include ParcelFormData for form submission"
  artifacts:
    - path: "src/lib/zod.ts"
      provides: "Form validation schemas matching backend Laravel rules"
      contains: "parcelSchema, parcelGeometrySchema, ParcelFormData"
    - path: "src/api/axios.ts"
      provides: "HTTP client with Laravel error formatting"
      exports: ["api"]
    - path: "src/api/types.ts"
      provides: "TypeScript types for API data"
      exports: ["ParcelFormData"]
  key_links:
    - from: "ParcelForm component (03-02)"
      to: "src/lib/zod.ts"
      via: "zodResolver(parcelSchema)"
      pattern: "zodResolver.*parcelSchema"
    - from: "mutation hooks (03-03, 03-06)"
      to: "src/api/axios.ts"
      via: "api.post/put/delete"
      pattern: "api\\.(post|put|delete)"
---

<objective>
Form Validation Foundation: Zod Schemas and API Error Handling

Purpose: Establish the validation layer for parcel forms using Zod schemas that mirror Laravel backend validation rules, and configure the Axios interceptor to convert Laravel 422 errors to react-hook-form format for inline field error display.

Output: Updated zod.ts with complete parcel schema, enhanced axios.ts with 422 error formatting, and API types for form data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-crud-operations/03-RESEARCH.md
@.planning/phases/03-crud-operations/03-UI-SPEC.md
@.planning/phases/03-crud-operations/03-VALIDATION.md

# Existing infrastructure from Phase 1 and Phase 2
@src/lib/zod.ts
@src/api/axios.ts
@src/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance Zod parcelSchema for full CRUD validation</name>
  <files>src/lib/zod.ts</files>
  <read_first>
    - src/lib/zod.ts
  </read_first>
  <action>
    Review the existing parcelSchema in src/lib/zod.ts and ensure it fully matches Laravel backend validation rules for CRUD operations. The schema must include:

    1. **owner_name**: string, required, min 1 char, max 255 chars, trimmed
    2. **status**: enum('free', 'negotiating', 'target'), required
    3. **price_per_sqm**: number, optional, min 0 (positive)
    4. **geometry**: GeoJSON Polygon with proper coordinate validation

    The existing schema already has these validations. Verify they are correct and add any missing refinements if needed.

    Key validation rules to confirm:
    - owner_name: .trim() removes whitespace
    - status: enum with exact Laravel enum values
    - price_per_sqm: .optional() allows null/undefined
    - geometry: validates RFC 7946 GeoJSON Polygon with closed ring (min 4 coordinates)

    No changes should be needed if the existing schema is correct. This task is a verification task to ensure the foundation is solid before building the form components.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/zod.test.ts</automated>
  </verify>
  <done>
    parcelSchema exists with all required fields (owner_name, status, price_per_sqm, geometry), owner_name has max 255 char constraint, status is enum with 'free', 'negotiating', 'target' values, price_per_sqm is optional with min 0, geometry uses parcelGeometrySchema
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Axios 422 error interceptor formatting</name>
  <files>src/api/axios.ts</files>
  <read_first>
    - src/api/axios.ts
  </read_first>
  <action>
    Review the existing Axios response interceptor in src/api/axios.ts and verify it correctly handles Laravel 422 validation errors. The interceptor should:

    1. Detect 422 status code from backend
    2. Parse Laravel error format: `{ errors: { field: ['message'] } }`
    3. Transform to react-hook-form format: `{ errors: { field: 'message' } }`
    4. Extract first message from array if present

    The existing interceptor should already have this logic. Verify the implementation and ensure error messages are properly formatted for react-hook-form setError() calls.

    Key implementation to verify:
    - `error.response?.status === 422` check
    - `data?.errors` iteration with array-to-string conversion
    - `Array.isArray(messages) ? messages[0] : messages` pattern
  </action>
  <verify>
    <automated>npm test -- --run src/api/__tests__/axios.test.ts</automated>
  </verify>
  <done>
    Axios interceptor correctly transforms Laravel 422 errors from array format to string format suitable for react-hook-form setError() calls, preserving field names and error messages
  </done>
</task>

<task type="auto">
  <name>Task 3: Export ParcelFormData type for form components</name>
  <files>src/lib/zod.ts, src/api/types.ts</files>
  <read_first>
    - src/lib/zod.ts
    - src/api/types.ts
  </read_first>
  <action>
    Ensure ParcelFormData type is properly exported from src/lib/zod.ts (it should already exist via z.infer). If it's not exported, add the export:

    ```typescript
    export type ParcelFormData = z.infer<typeof parcelSchema>
    ```

    Also verify src/api/types.ts has this type re-exported or imported from zod.ts for consistency across the codebase. Add an import/re-export if needed:

    ```typescript
    export type { ParcelFormData } from '@/lib/zod'
    ```

    This ensures form components can import ParcelFormData from either location without confusion.
  </action>
  <verify>
    <automated>npm test -- --run src/lib/__tests__/zod.test.ts</automated>
  </verify>
  <done>
    ParcelFormData type is exported from src/lib/zod.ts via z.infer, and is accessible to form components for type-safe form handling with react-hook-form
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Form data crosses to backend via POST/PUT requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | parcelSchema | mitigate | Zod schema validates all input on client-side (defense-in-depth), with server-side Laravel validation as authoritative. owner_name max 255 chars prevents DoS via long strings. |
| T-03-01-02 | Tampering | axios.ts error formatter | mitigate | Error messages formatted as plain text (not HTML), React auto-escapes when rendering. No raw HTML from backend reaches DOM. |
| T-03-01-03 | Tampering | price_per_sqm validation | mitigate | Zod enforces min(0) to prevent negative prices, number type prevents non-numeric input. |
| T-03-01-04 | Tampering | geometry schema | mitigate | GeoJSON RFC 7946 validation via parcelGeometrySchema ensures coordinate bounds (-180 to 180 lng, -90 to 90 lat) and polygon closure. |
| T-03-01-05 | Information Disclosure | API types | accept | Types only define data structure, no sensitive data exposed. No credentials or secrets in type definitions. |
</threat_model>

<verification>
- parcelSchema validates all required fields with proper constraints
- Axios interceptor transforms 422 errors correctly
- ParcelFormData type is exported and accessible
- TypeScript compilation succeeds
</verification>

<success_criteria>
1. Zod parcelSchema validates owner_name (1-255 chars), status (enum), price_per_sqm (optional, >=0), geometry (valid GeoJSON Polygon)
2. Axios interceptor converts Laravel 422 errors from array to string format
3. ParcelFormData type is exported from zod.ts
4. Form components can use zodResolver(parcelSchema) for validation
5. TypeScript compilation succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud-operations/03-01-SUMMARY.md`
</output>
