---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [vitest.config.ts, src/test/setup.ts, package.json]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Vitest is configured with jsdom environment and @testing-library/react"
    - "Test setup file provides cleanup utilities for each test"
    - "npm test command runs without errors"
    - "npm run test:all command runs with coverage"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration for testing React components"
      min_lines: 20
    - path: "src/test/setup.ts"
      provides: "Test setup with cleanup utilities"
      min_lines: 10
    - path: "package.json"
      provides: "Test scripts and testing library dependencies"
      contains: '"test": "vitest"'
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles configuration"
      pattern: "setupFiles.*setup"
---

<objective>
Establish test infrastructure foundation with Vitest configuration, test setup utilities, and testing library dependencies. This Wave 0 plan enables all subsequent tasks to have automated verification per Nyquist rules.

Purpose: Enable automated verification for all subsequent Phase 1 tasks
Output: Working Vitest configuration with test utilities
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install testing dependencies and configure Vitest</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    <file>package.json</file>
  </read_first>
  <action>
    Install testing dependencies:
    ```bash
    npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom vitest @vitest/ui
    ```

    Create vitest.config.ts in project root:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      plugins: [react()],
      test: {
        globals: true,
        environment: 'jsdom',
        setupFiles: './src/test/setup.ts',
        css: true,
        coverage: {
          provider: 'v8',
          reporter: ['text', 'json', 'html'],
        },
      },
    })
    ```

    Update package.json scripts to include:
    ```json
    "scripts": {
      "test": "vitest",
      "test:all": "vitest --coverage",
      "test:ui": "vitest --ui"
    }
    ```

    Use the exact package versions from RESEARCH.md:
    - vitest (latest compatible with Vite 8.0.8)
    - @testing-library/react (latest for React 19)
    - jsdom (latest)
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | head -20</automated>
  </verify>
  <done>
    - vitest.config.ts exists with jsdom environment
    - package.json has test, test:all, test:ui scripts
    - `npm test -- --run` executes without errors (may report no tests)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test setup file with cleanup utilities</name>
  <files>src/test/setup.ts</files>
  <read_first>
    <file>src/test/setup.ts</file>
  </read_first>
  <action>
    Create src/test/setup.ts with the following content:

    ```typescript
    import { expect, afterEach } from 'vitest'
    import { cleanup } from '@testing-library/react'
    import * as matchers from '@testing-library/jest-dom/matchers'

    // Extend Vitest's expect with jest-dom matchers
    expect.extend(matchers)

    // Cleanup after each test
    afterEach(() => {
      cleanup()
    })
    ```

    This provides:
    - Automatic cleanup after each test (prevents memory leaks)
    - jest-dom matchers like toBeInTheDocument(), toHaveClass(), etc.
  </action>
  <verify>
    <automated>test -f src/test/setup.ts && grep -q "cleanup()" src/test/setup.ts</automated>
  </verify>
  <done>
    - src/test/setup.ts exists
    - File imports cleanup from @testing-library/react
    - File calls afterEach() with cleanup()
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Tests import and execute production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-00-01 | Tampering | Test environment | accept | Test code cannot affect production deployment; tests run in isolation |
| T-01-00-02 | Information Disclosure | Coverage reports | accept | Coverage reports only in CI/CD, not exposed in production |
</threat_model>

<verification>
After completion:
1. Run `npm test -- --run` - should execute without errors
2. Run `npm run test:all` - should generate coverage report
3. Verify vitest.config.ts exists with jsdom environment
</verification>

<success_criteria>
- Vitest configured with jsdom environment
- Test setup provides cleanup utilities
- Test scripts (test, test:all, test:ui) available in package.json
- All testing dependencies installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
