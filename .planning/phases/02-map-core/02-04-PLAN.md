---
phase: 02-map-core
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/components/map/ParcelSidebar.tsx
autonomous: true
requirements:
  - MAP-03
user_setup: []

must_haves:
  truths:
    - "ParcelSidebar renders when isOpen is true"
    - "Sidebar displays parcel details (owner, status, area, price)"
    - "Sidebar has close button that triggers onClose callback"
    - "Sidebar animates in from right with 300ms transition"
  artifacts:
    - path: "src/components/map/ParcelSidebar.tsx"
      provides: "Slide-in sidebar for parcel details"
      exports: ["ParcelSidebar"]
      min_lines: 60
  key_links:
    - from: "src/components/map/ParcelSidebar.tsx"
      to: "src/api/types.ts"
      via: "import type { ParcelFeature }"
      pattern: "ParcelFeature"
    - from: "src/components/map/ParcelSidebar.tsx"
      to: "src/lib/utils.ts"
      via: "import { formatArea, getParcelColor, formatDate }"
      pattern: "formatArea\|getParcelColor"
---

<objective>
Create ParcelSidebar component that slides in from the right to display selected parcel details.

Purpose: Implements the parcel detail view (MAP-03) that appears when a user clicks a polygon. The sidebar shows formatted parcel data with proper animation per UI-SPEC.
Output: ParcelSidebar component with slide-in animation, parcel details display, and close functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-map-core/02-RESEARCH.md
@.planning/phases/02-map-core/02-UI-SPEC.md
@src/api/types.ts
@src/lib/utils.ts

<interfaces>
From src/api/types.ts:
```typescript
export type ParcelFeature = Feature<Polygon, ParcelProperties>
export interface ParcelProperties {
  id: number
  owner_name: string
  status: ParcelStatus
  price_per_sqm: number | null
  area_sqm: number
  created_at: string
  updated_at: string
  [key: string]: any
}
export type ParcelStatus = 'free' | 'negotiating' | 'target'
```

From src/lib/utils.ts:
```typescript
export function getParcelColor(status: ParcelStatus): string
export function formatArea(sqm: number): string
export function formatPrice(pricePerSqm: number | null, areaSqm: number): string
export function formatDate(dateString: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ParcelSidebar component with slide-in animation</name>
  <files>src/components/map/ParcelSidebar.tsx</files>
  <read_first>
    - src/api/types.ts (verify ParcelFeature type)
    - src/lib/utils.ts (verify formatting functions)
  </read_first>
  <action>
    Create ParcelSidebar component per UI-SPEC.md specifications:

    File: src/components/map/ParcelSidebar.tsx

    ```typescript
    import { X } from 'lucide-react'
    import type { ParcelFeature } from '@/api/types'
    import { formatArea, formatPrice, formatDate, getParcelColor } from '@/lib/utils'

    interface ParcelSidebarProps {
      parcel: ParcelFeature | null
      isOpen: boolean
      onClose: () => void
    }

    export function ParcelSidebar({ parcel, isOpen, onClose }: ParcelSidebarProps) {
      if (!parcel) return null

      const properties = parcel.properties
      const statusColor = getParcelColor(properties.status)

      return (
        <aside
          className={`fixed top-0 right-0 bottom-0 z-20 w-80 bg-white shadow-xl transition-transform duration-300 ease-in-out ${
            isOpen ? 'translate-x-0' : 'translate-x-full'
          }`}
        >
          {/* Header with close button */}
          <div className="flex items-center justify-between border-b border-slate-200 px-4 py-4">
            <h2 className="text-lg font-semibold text-slate-900">Parcel Details</h2>
            <button
              onClick={onClose}
              className="rounded p-1 text-slate-500 transition-colors hover:bg-slate-100 hover:text-slate-700"
              aria-label="Close sidebar"
            >
              <X className="h-5 w-5" />
            </button>
          </div>

          {/* Parcel details */}
          <div className="px-4 py-4">
            {/* Status badge */}
            <div className="mb-4">
              <span
                className="inline-block rounded-full px-3 py-1 text-sm font-medium text-white"
                style={{ backgroundColor: statusColor }}
              >
                {properties.status.charAt(0).toUpperCase() + properties.status.slice(1)}
              </span>
            </div>

            {/* Owner name */}
            <div className="mb-4">
              <label className="mb-1 block text-sm font-medium text-slate-700">
                Owner
              </label>
              <p className="text-slate-900">{properties.owner_name}</p>
            </div>

            {/* Area */}
            <div className="mb-4">
              <label className="mb-1 block text-sm font-medium text-slate-700">
                Area
              </label>
              <p className="text-slate-900">{formatArea(properties.area_sqm)}</p>
            </div>

            {/* Price */}
            <div className="mb-4">
              <label className="mb-1 block text-sm font-medium text-slate-700">
                Price
              </label>
              <p className="text-slate-900">
                {formatPrice(properties.price_per_sqm, properties.area_sqm)}
              </p>
              {properties.price_per_sqm !== null && (
                <p className="mt-1 text-sm text-slate-500">
                  IDR {properties.price_per_sqm.toLocaleString('id-ID')}/m²
                </p>
              )}
            </div>

            {/* Created date */}
            <div className="mb-4">
              <label className="mb-1 block text-sm font-medium text-slate-700">
                Created
              </label>
              <p className="text-sm text-slate-600">
                {formatDate(properties.created_at)}
              </p>
            </div>

            {/* Updated date */}
            <div>
              <label className="mb-1 block text-sm font-medium text-slate-700">
                Last Updated
              </label>
              <p className="text-sm text-slate-600">
                {formatDate(properties.updated_at)}
              </p>
            </div>
          </div>
        </aside>
      )
    }
    ```

    Per UI-SPEC.md sidebar specifications:
    - Width: 320px (w-80)
    - Background: white
    - Padding: 24px vertical, 16px horizontal (p-4)
    - Shadow: shadow-xl
    - Animation: 300ms ease-in-out
    - States: translate-x-full (closed), translate-x-0 (open)
    - Z-index: 20 (above map and overlays)

    Status badge colors:
    - Uses getParcelColor() for background
    - Capitalized status text
  </action>
  <verify>
    <automated>test -f src/components/map/ParcelSidebar.tsx && grep -q "translate-x-0\|translate-x-full" src/components/map/ParcelSidebar.tsx && grep -q "duration-300" src/components/map/ParcelSidebar.tsx && grep -q "w-80" src/components/map/ParcelSidebar.tsx</automated>
  </verify>
  <done>
    ParcelSidebar.tsx exists with slide-in animation, parcel details (status badge, owner, area, price, dates), and close button
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → ParcelSidebar | Parcel properties displayed to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | ParcelSidebar content | mitigate | React auto-escapes all properties; formatArea/formatPrice/formatDate handle null values safely |
| T-02-11 | Information Disclosure | Parcel data in sidebar | accept | Public API, no sensitive data in v1 (per STATE.md) |

**Mitigation in place:** React's automatic escaping prevents XSS from parcel properties.
</threat_model>

<verification>
After all tasks complete:
1. Verify ParcelSidebar imports all required utility functions
2. Verify sidebar has correct width (w-80) and z-index (z-20)
3. Verify animation classes are present (translate-x, duration-300)
4. Run npm run build to verify no compilation errors
</verification>

<success_criteria>
1. ParcelSidebar renders when isOpen is true
2. Sidebar is hidden (translate-x-full) when isOpen is false
3. Status badge uses correct color per parcel status
4. All parcel details are formatted correctly
5. Close button calls onClose callback
</success_criteria>

<output>
After completion, create `.planning/phases/02-map-core/02-04-SUMMARY.md`
</output>
