---
phase: 04-spatial-analysis
plan: 03
type: execute
wave: 2
depends_on: [04-00, 04-01]
files_modified: [src/hooks/useBufferAnalysis.ts, src/components/map/BufferPanel.tsx, src/components/map/ParcelSidebar.tsx]
autonomous: true
requirements: [ANA-01, ANA-02, ANA-03, ANA-06]
user_setup: []
must_haves:
  truths:
    - "User can click 'Analyze Nearby' button in parcel sidebar"
    - "Buffer panel appears with distance input (default 500m)"
    - "User can adjust distance from 1-10000 meters"
    - "Applying buffer queries API and shows nearby parcels"
    - "Sidebar shows count of parcels found within buffer"
  artifacts:
    - path: "src/hooks/useBufferAnalysis.ts"
      provides: "React Query hook for buffer analysis"
      min_lines: 50
      exports: ["useBufferAnalysis"]
    - path: "src/components/map/BufferPanel.tsx"
      provides: "Floating panel with distance input and apply/cancel buttons"
      min_lines: 70
      exports: ["BufferPanel"]
  key_links:
    - from: "src/hooks/useBufferAnalysis.ts"
      to: "src/api/axios.ts"
      via: "api.post('/buffer')"
      pattern: "api\\.post\\('/buffer'"
    - from: "src/hooks/useBufferAnalysis.ts"
      to: "src/lib/zod.ts"
      via: "bufferRequestSchema validation"
      pattern: "bufferRequestSchema\\.parse\\("
    - from: "src/components/map/BufferPanel.tsx"
      to: "src/hooks/useBufferAnalysis.ts"
      via: "useBufferAnalysis hook"
      pattern: "useBufferAnalysis\\(center, radius\\)"
---

<objective>
Implement buffer analysis functionality for finding nearby parcels. Users can perform buffer analysis from a selected parcel or from an arbitrary point on the map, with a distance input control and results display.

Purpose: Enable spatial proximity analysis to find parcels within a specified radius of a point.

Output: useBufferAnalysis hook and BufferPanel component with distance input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spatial-analysis/04-RESEARCH.md
@.planning/phases/04-spatial-analysis/04-UI-SPEC.md
@.planning/phases/03-crud-operations/03-06-SUMMARY.md (mutation hooks pattern)
@src/api/types.ts (for BufferResult type)
@src/lib/zod.ts (for bufferRequestSchema)
@src/api/axios.ts (for api instance)
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/api/types.ts:
```typescript
export interface BufferPoint extends Point {
  coordinates: [number, number] // [lng, lat] per GeoJSON spec
}

export interface BufferResult {
  center: BufferPoint
  radius: number
  parcels: ParcelCollection
}
```

From src/lib/zod.ts:
```typescript
export const bufferRequestSchema = z.object({
  parcel_id: z.number().int().positive().optional(),
  lat: z.number().min(-90).max(90),
  lng: z.number().min(-180).max(180),
  distance: z.number().min(1).max(10000).default(500),
})

export type BufferRequestData = z.infer<typeof bufferRequestSchema>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useBufferAnalysis hook for buffer queries</name>
  <files>src/hooks/useBufferAnalysis.ts</files>
  <read_first>
    - src/hooks/useCreateParcel.tsx (for React Query mutation pattern)
    - src/lib/zod.ts (for bufferRequestSchema)
    - src/api/types.ts (for BufferResult type)
  </read_first>
  <behavior>
    - Hook accepts center: L.LatLng | null, radius: number
    - Returns { data: BufferResult | null, isLoading, error, refetch }
    - Query is enabled only when center is not null
    - Request validated with bufferRequestSchema before sending
    - Query posts to /api/v1/buffer endpoint
    - staleTime is 5 minutes (spatial data is stable)
  </behavior>
  <action>
    Create src/hooks/useBufferAnalysis.ts with the following:

    1. Import { useQuery } from '@tanstack/react-query'
    2. Import { api } from '@/api/axios'
    3. Import type { BufferResult } from '@/api/types'
    4. Import { bufferRequestSchema } from '@/lib/zod'
    5. Import L from 'leaflet' for LatLng type
    6. Import toast from 'react-hot-toast'
    7. Import { CheckCircle, XCircle } from 'lucide-react'

    Hook signature:
    ```typescript
    export function useBufferAnalysis(center: L.LatLng | null, radius: number)
    ```

    Implementation:
    - Return useQuery<BufferResult>({
        queryKey: ['buffer', center?.lat, center?.lng, radius],
        queryFn: async () => {
          if (!center) throw new Error('Center required')

          const requestData = {
            lat: center.lat,
            lng: center.lng,
            distance: radius,
          }

          // Validate with Zod
          const validated = bufferRequestSchema.parse(requestData)

          const { data } = await api.post<BufferResult>('/buffer', validated)
          return data
        },
        enabled: !!center && radius > 0 && radius <= 10000,
        staleTime: 1000 * 60 * 5, // 5 minutes
      })

    Return { data, isLoading, error, refetch } from useQuery result.

    NOTE: Error handling is via React Query error state; toast notifications will be handled by calling component.
  </action>
  <verify>
    <automated>npm test -- --run useBufferAnalysis.test.tsx</automated>
  </verify>
  <done>
    - File src/hooks/useBufferAnalysis.ts exists
    - Hook accepts center and radius parameters
    - Query enabled only when center is set
    - Request validated with bufferRequestSchema
    - Posts to /buffer endpoint
    - Returns BufferResult data
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BufferPanel component with distance input</name>
  <files>src/components/map/BufferPanel.tsx</files>
  <read_first>
    - src/components/map/DrawingToolbar.tsx (for floating control panel pattern)
    - .planning/phases/04-spatial-analysis/04-UI-SPEC.md (Buffer Analysis Controls section)
    - src/lib/zod.ts (for distance range 1-10000)
  </read_first>
  <behavior>
    - Renders floating panel with distance input
    - Input type number with min 1, max 10000, default 500
    - Apply button triggers onApply with distance
    - Cancel button triggers onCancel
    - Panel positioned top-right of map (z-15)
    - White background with shadow, rounded corners
  </behavior>
  <action>
    Create src/components/map/BufferPanel.tsx with the following:

    Props interface:
    ```typescript
    interface BufferPanelProps {
      initialRadius?: number
      onApply: (radius: number) => void
      onCancel: () => void
    }
    ```

    Component structure:
    - Container: "absolute top-4 right-4 z-15 bg-white shadow-lg rounded-lg p-3 w-64"
    - Label: "Buffer Radius" with "text-sm font-medium text-slate-700"
    - Input container: flex items-center gap-2
    - Input: type number, min 1, max 10000, step 10, defaultValue initialRadius
      * Classes: "rounded-md border border-slate-200 px-3 py-1.5 text-sm flex-1"
      * Focus: border-blue-500 ring-2 ring-blue-500/20 outline-none
    - Unit: "meters" with text-slate-500 text-sm
    - Buttons container: flex items-center gap-2 mt-3
    - Apply button: bg-blue-500 text-white, px-3 py-1.5 rounded-md text-sm
      * Icon: Check from lucide-react
    - Cancel button: bg-slate-100 text-slate-700, px-3 py-1.5 rounded-md text-sm
      * Icon: X from lucide-react

    State management:
    - useState for distance value (initialize with initialRadius || 500)
    - onApply: validate distance (1-10000), call onApply(distance)
    - onCancel: call onCancel()

    Import Check, X icons from lucide-react.
  </action>
  <verify>
    <automated>npm test -- --run BufferPanel.test.tsx</automated>
  </verify>
  <done>
    - File src/components/map/BufferPanel.tsx exists
    - Panel renders with distance input
    - Input accepts 1-10000 meter range
    - Apply button calls onApply with distance
    - Cancel button calls onCancel
    - Panel styled per UI-SPEC
  </done>
</task>

<task type="auto">
  <name>Task 3: Add buffer mode to ParcelSidebar and integrate with hook</name>
  <files>src/components/map/ParcelSidebar.tsx</files>
  <read_first>
    - src/components/map/ParcelSidebar.tsx (current sidebar structure)
    - src/hooks/useBufferAnalysis.ts (buffer hook)
    - src/components/map/BufferPanel.tsx (buffer panel)
  </read_first>
  <action>
    Update ParcelSidebar.tsx:

    1. Add new mode: 'buffer' to SidebarMode type
    2. Add new props: bufferResult?: BufferResult | null, onBufferStart?: () => void, onBufferApply?: (radius: number) => void
    3. Add "Analyze Nearby" button in view mode (below details, above edit/delete)
       * Button: "Analyze Nearby" with Radio icon from lucide-react
       * Classes: "w-full bg-blue-500 text-white hover:bg-blue-600 rounded-md py-2 text-sm font-medium"
       * Visible only in view mode
       * onClick: calls onBufferStart or switches to buffer mode

    4. Add buffer mode rendering:
       * Header: "Nearby Parcels" with subheading "{count} parcels within {radius}m"
       * Buffer results list (placeholder for now, full implementation in Plan 04)
       * Each result shows: owner name, distance, status badge
       * Click on result opens that parcel in view mode

    5. Import BufferResult type from '@/api/types'
    6. Import Radio icon from lucide-react

    For buffer results list (minimal implementation):
    - Map through bufferResult?.parcels.features
    - Show owner_name, distance (if available), status badge
    - Click calls onParcelClick with parcel id

    Status badge styling (per UI-SPEC):
    - Free: bg-green-100 text-green-700
    - Negotiating: bg-yellow-100 text-yellow-700
    - Target: bg-red-100 text-red-700
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - ParcelSidebar has 'buffer' mode
    - "Analyze Nearby" button appears in view mode
    - Buffer mode shows results header with count
    - Results list renders nearby parcels
    - Clicking result opens parcel in view mode
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Buffer request data (lat, lng, distance) sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T (Tampering) | buffer distance | mitigate | Use existing bufferRequestSchema with 1-10000m range validation at hook entry before API call |
| T-04-03-02 | T (Tampering) | buffer coordinates | accept | Coordinates from Leaflet (trusted) or selected parcel (from API) |
</threat_model>

<verification>
1. Click parcel to open sidebar
2. Click "Analyze Nearby" button
3. Verify BufferPanel appears with 500m default
4. Change distance and click Apply
5. Verify API is called with correct parameters
6. Verify results show in sidebar
7. Run `npm test -- --run useBufferAnalysis.test.tsx BufferPanel.test.tsx`
8. Run `npm run build`
</verification>

<success_criteria>
- "Analyze Nearby" button appears in parcel sidebar
- BufferPanel appears with distance input (default 500m)
- Distance input validates 1-10000m range
- Apply button triggers buffer query
- Sidebar shows "X parcels within Ym" header
- Nearby parcels listed in sidebar
- Clicking nearby parcel opens its details
</success_criteria>

<output>
After completion, create `.planning/phases/04-spatial-analysis/04-03-SUMMARY.md`
</output>
