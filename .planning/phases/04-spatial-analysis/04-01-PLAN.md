---
phase: 04-spatial-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/map/FilterBar.tsx, src/hooks/useFilterParams.ts, src/lib/url-utils.ts, src/components/map/MapHeader.tsx]
autonomous: true
requirements: [FLT-01, FLT-03, FLT-04]
user_setup: []
must_haves:
  truths:
    - "User can filter parcels by clicking status buttons (free/negotiating/target)"
    - "URL updates with ?status parameter when filters change"
    - "Opening a shared URL restores the exact filter state"
    - "Filter buttons show active state (filled color) when selected"
  artifacts:
    - path: "src/components/map/FilterBar.tsx"
      provides: "Status filter buttons with active states"
      min_lines: 60
      exports: ["FilterBar"]
    - path: "src/hooks/useFilterParams.ts"
      provides: "URL parameter synchronization hook"
      min_lines: 80
      exports: ["useFilterParams"]
    - path: "src/lib/url-utils.ts"
      provides: "URL parsing utilities (parseBbox, parseStatus)"
      min_lines: 40
      exports: ["parseBbox", "parseStatus", "buildUrlParams"]
  key_links:
    - from: "src/components/map/FilterBar.tsx"
      to: "src/hooks/useFilterParams.ts"
      via: "useFilterParams hook for state management"
      pattern: "useFilterParams\\(\\)"
    - from: "src/hooks/useFilterParams.ts"
      to: "window.location.search"
      via: "URLSearchParams API"
      pattern: "new URLSearchParams\\(window\\.location\\.search\\)"
---

<objective>
Implement status filter UI with URL parameter synchronization. Users can filter parcels by clicking status buttons (Free/Negotiating/Target), with filter state reflected in URL for shareable links.

Purpose: Provide the primary filtering mechanism for parcels with shareable filter state via URL parameters.

Output: FilterBar component with status buttons and useFilterParams hook for URL synchronization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spatial-analysis/04-RESEARCH.md
@.planning/phases/04-spatial-analysis/04-UI-SPEC.md
@.planning/phases/03-crud-operations/03-07-SUMMARY.md
@src/lib/utils.ts (for cn utility, STATUS_COLORS)
@src/api/types.ts (for ParcelStatus type)
@src/components/map/MapHeader.tsx (for integration)
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/api/types.ts:
```typescript
export type ParcelStatus = 'free' | 'negotiating' | 'target'
```

From src/lib/utils.ts:
```typescript
export function cn(...classes: (string | undefined | null | false)[]): string
export const STATUS_COLORS: Record<ParcelStatus, string>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create URL utility functions for parsing and building params</name>
  <files>src/lib/url-utils.ts</files>
  <read_first>
    - src/lib/utils.ts (for existing utility patterns)
    - src/api/types.ts (for ParcelStatus type)
  </read_first>
  <behavior>
    - parseStatus('free,negotiating') returns ['free', 'negotiating']
    - parseStatus(null) returns []
    - parseStatus('invalid') throws error or returns []
    - parseBbox('106.8,-6.2,106.9,-6.1') returns Leaflet LatLngBounds
    - parseBbox(null) returns null
    - parseBbox('invalid') returns null (defensive)
    - buildUrlParams({ status: [], bbox: null, selected: null }) returns empty URLSearchParams
    - buildUrlParams with status adds ?status=free,negotiating
  </behavior>
  <action>
    Create src/lib/url-utils.ts with the following functions:

    1. parseStatus(statusParam: string | null): ParcelStatus[]
       - Split by comma if not null
       - Filter to valid ParcelStatus values ('free', 'negotiating', 'target')
       - Return empty array if null or invalid

    2. parseBbox(bboxParam: string | null): L.LatLngBounds | null
       - Import L from 'leaflet'
       - Split by comma: [minLng, minLat, maxLng, maxLat]
       - Validate 4 numeric values
       - Return L.latLngBounds([minLat, minLng], [maxLat, maxLng]) or null

    3. buildUrlParams(filters: FilterState): URLSearchParams
       - FilterState interface: { status: ParcelStatus[], bbox: L.LatLngBounds | null, selected: number | null }
       - Create URLSearchParams instance
       - Add status if array not empty: join with comma
       - Add bbox if not null: format as 'minLng,minLat,maxLng,maxLat'
       - Add selected if not null
       - Return URLSearchParams

    Export all functions with proper TypeScript types.
  </action>
  <verify>
    <automated>npm test -- --run url-utils.test.ts || echo "Test file not yet created, verify manually"</automated>
  </verify>
  <done>
    - File src/lib/url-utils.ts exists
    - Exports parseStatus, parseBbox, buildUrlParams
    - parseStatus handles comma-separated values and null
    - parseBbox creates valid LatLngBounds or null
    - buildUrlParams creates URLSearchParams from filter state
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useFilterParams hook for URL synchronization</name>
  <files>src/hooks/useFilterParams.ts</files>
  <read_first>
    - src/hooks/useParcels.ts (for React Query pattern)
    - src/lib/url-utils.ts (for parse/build functions)
    - src/api/types.ts (for ParcelStatus type)
  </read_first>
  <behavior>
    - Hook returns { filters, setFilters, clearFilters }
    - filters.status starts as [] (no filter)
    - filters.bbox starts as null
    - filters.selected starts as null
    - URL params parsed on mount (empty dependency useEffect)
    - URL updates when filters change (useEffect with filters deps)
    - clearFilters resets all to default and clears URL
  </behavior>
  <action>
    Create src/hooks/useFilterParams.ts with the following:

    1. Import useState, useEffect, useCallback from 'react'
    2. Import { parseBbox, parseStatus, buildUrlParams } from '@/lib/url-utils'
    3. Import type { ParcelStatus } from '@/api/types'

    Define FilterState interface:
    ```typescript
    interface FilterState {
      status: ParcelStatus[]
      bbox: L.LatLngBounds | null
      selected: number | null
    }
    ```

    Hook implementation:
    - Initialize state with default: { status: [], bbox: null, selected: null }
    - Parse URL on mount with useEffect (empty deps):
      * Use URLSearchParams(window.location.search)
      * Call parseStatus, parseBbox, parseInt for selected
      * Set parsed state

    - Sync to URL with useEffect (filters deps):
      * Call buildUrlParams(filters)
      * Build new URL with pathname + params
      * Use window.history.replaceState to update URL

    - clearFilters callback resets to default state
    - Return { filters, setFilters, clearFilters }

    NOTE: Import L from 'leaflet' for LatLngBounds type.
  </action>
  <verify>
    <automated>npm test -- --run useFilterParams.test.tsx</automated>
  </verify>
  <done>
    - File src/hooks/useFilterParams.ts exists
    - Exports useFilterParams hook
    - Hook returns filters, setFilters, clearFilters
    - URL parsed on mount
    - URL updated when filters change
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create FilterBar component with status buttons</name>
  <files>src/components/map/FilterBar.tsx</files>
  <read_first>
    - src/components/map/MapHeader.tsx (for existing header component pattern)
    - src/lib/utils.ts (for cn utility, STATUS_COLORS)
    - src/hooks/useFilterParams.ts (for filter state)
  </read_first>
  <behavior>
    - Renders three status buttons: Free, Negotiating, Target
    - Buttons use cn() for conditional classes
    - Active buttons: bg-{status}-500 text-white (filled color)
    - Inactive buttons: bg-slate-100 text-slate-700 hover:bg-slate-200
    - Click toggles status in filter (add if not present, remove if present)
    - Clear Filters button shown when any status is active
    - Clear button calls clearFilters from hook
  </behavior>
  <action>
    Create src/components/map/FilterBar.tsx with the following:

    Props interface:
    ```typescript
    interface FilterBarProps {
      activeStatuses: ParcelStatus[]
      onStatusToggle: (status: ParcelStatus) => void
      onClear: () => void
    }
    ```

    Component structure:
    - Container div with classes: "flex items-center gap-3 border-b border-slate-200 bg-white px-4 py-2"
    - Filter icon (lucide-react Filter) in slate-600
    - Three status buttons mapped from ['free', 'negotiating', 'target']
    - Clear Filters button (shown when activeStatuses.length > 0)

    Status button classes (use cn utility):
    - Base: "rounded-md px-3 py-1.5 text-sm font-medium transition-colors"
    - Active (free): "bg-green-500 text-white"
    - Active (negotiating): "bg-yellow-500 text-white"
    - Active (target): "bg-red-500 text-white"
    - Inactive: "bg-slate-100 text-slate-700 hover:bg-slate-200"

    Clear button:
    - Icon: X from lucide-react
    - Text: "Clear Filters"
    - Classes: "flex items-center gap-1 text-sm font-medium text-slate-600 hover:text-slate-900"

    Import Filter, X icons from lucide-react.
    Import cn utility from '@/lib/utils'.
  </action>
  <verify>
    <automated>npm test -- --run FilterBar.test.tsx</automated>
  </verify>
  <done>
    - File src/components/map/FilterBar.tsx exists
    - Component renders three status buttons
    - Buttons show correct active/inactive styling
    - Clear button appears when filters active
    - Click handlers call provided callbacks
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate FilterBar with MapHeader and add filter state to App</name>
  <files>src/components/map/MapHeader.tsx, src/App.tsx</files>
  <read_first>
    - src/components/map/MapHeader.tsx (current header structure)
    - src/App.tsx (current state management)
    - src/hooks/useFilterParams.ts (filter hook)
  </read_first>
  <action>
    Update MapHeader.tsx:
    1. Add new props: showFilterBar: boolean (default false)
    2. Add FilterBar component below header (inside header container or as separate row)
    3. When showFilterBar is true, render FilterBar with filter state

    Update App.tsx:
    1. Import useFilterParams from '@/hooks/useFilterParams'
    2. Call hook: const { filters, setFilters, clearFilters } = useFilterParams()
    3. Add handleStatusToggle function:
       ```typescript
       const handleStatusToggle = (status: ParcelStatus) => {
         const newStatuses = filters.status.includes(status)
           ? filters.status.filter(s => s !== status)
           : [...filters.status, status]
         setFilters({ ...filters, status: newStatuses })
       }
       ```
    4. Pass showFilterBar={true} to MapHeader
    5. Pass activeStatuses, onStatusToggle, onClear to FilterBar via MapHeader props
    6. Update MapHeader props interface to accept filter props

    For MapHeader filter props:
    - filterBarProps?: { activeStatuses: ParcelStatus[], onStatusToggle: (status: ParcelStatus) => void, onClear: () => void }
    - Destructure and pass to FilterBar when present
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - MapHeader accepts filter props
    - FilterBar renders in header when showFilterBar is true
    - App.tsx uses useFilterParams hook
    - Clicking status buttons updates URL
    - Clear button resets filters and URL
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→URL | User input crosses into URL parameters |
| URL→client | URL params parsed and applied to state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Injection) | parseBbox, parseStatus | mitigate | Validate status enum values; parse bbox coordinates with parseFloat and NaN check before creating LatLngBounds |
| T-04-02 | T (Tampering) | URL params | accept | URL params contain only public data (status enum, coordinates); no sensitive information exposed |
| T-04-03 | I (Injection) | buildUrlParams | accept | URLSearchParams API auto-encodes values; no raw string concatenation |
</threat_model>

<verification>
1. Test status filter buttons toggle correctly
2. Verify URL updates to ?status=free,negotiating format
3. Test URL restoration by manually adding params and refreshing
4. Run `npm test -- --run FilterBar.test.tsx useFilterParams.test.tsx`
5. Run `npm run build` to verify TypeScript compilation
</verification>

<success_criteria>
- Status buttons filter parcels when clicked
- URL updates with ?status parameter
- Reloading page with ?status=free restores filter state
- Clear filters button resets all state and URL
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-spatial-analysis/04-01-SUMMARY.md`
</output>
