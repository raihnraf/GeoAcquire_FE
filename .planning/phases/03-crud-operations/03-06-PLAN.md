---
phase: 03-crud-operations
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-03", "03-05"]
files_modified:
  - src/hooks/useUpdateParcel.ts
  - src/hooks/useDeleteParcel.ts
  - src/components/map/DeleteConfirmModal.tsx
autonomous: true
requirements: [CRUD-04, CRUD-05, CRUD-08, CRUD-09]
user_setup: []

must_haves:
  truths:
    - "useUpdateParcel hook puts to /parcels/{id} endpoint"
    - "useDeleteParcel hook deletes /parcels/{id} endpoint"
    - "DeleteConfirmModal shows warning with AlertTriangle icon"
    - "Both hooks invalidate parcels query on success"
  artifacts:
    - path: "src/hooks/useUpdateParcel.ts"
      provides: "React Query mutation hook for updating parcels"
      min_lines: 30
    - path: "src/hooks/useDeleteParcel.ts"
      provides: "React Query mutation hook for deleting parcels"
      min_lines: 30
    - path: "src/components/map/DeleteConfirmModal.tsx"
      provides: "Confirmation modal for delete action"
      min_lines: 50
  key_links:
    - from: "useUpdateParcel"
      to: "src/api/axios.ts"
      via: "api.put('/parcels/{id}')"
      pattern: "api\\.put.*parcels"
    - from: "useDeleteParcel"
      to: "src/api/axios.ts"
      via: "api.delete('/parcels/{id}')"
      pattern: "api\\.delete.*parcels"
    - from: "DeleteConfirmModal"
      to: "lucide-react"
      via: "AlertTriangle icon"
      pattern: "AlertTriangle"
</key_links>
---

<objective>
Edit and Delete: Mutation Hooks and Confirmation Modal

Purpose: Implement useUpdateParcel and useDeleteParcel React Query mutation hooks with cache invalidation and toast notifications, and DeleteConfirmModal component for delete confirmation dialog.

Output: useUpdateParcel.ts, useDeleteParcel.ts hooks and DeleteConfirmModal.tsx component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-crud-operations/03-RESEARCH.md
@.planning/phases/03-crud-operations/03-UI-SPEC.md
@src/api/axios.ts
@src/lib/zod.ts

# Patterns from RESEARCH.md
- React Query useMutation with cache invalidation
- react-hot-toast for notifications
- Modal pattern with overlay and confirmation buttons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useUpdateParcel mutation hook</name>
  <files>src/hooks/useUpdateParcel.ts</files>
  <read_first>
    - src/api/axios.ts
    - src/lib/zod.ts
    - src/hooks/useCreateParcel.ts
  </read_first>
  <action>
    Create src/hooks/useUpdateParcel.ts as a React Query mutation hook for updating parcels.

    Implementation requirements:
    1. Import useMutation, useQueryClient from '@tanstack/react-query'
    2. Import api from '@/api/axios'
    3. Import ParcelFormData from '@/lib/zod'
    4. Import toast from 'react-hot-toast'
    5. Import CheckCircle and XCircle from 'lucide-react'
    6. Use useQueryClient to get queryClient instance
    7. Return useMutation with:
       - mutationFn: async ({ id, data }: { id: number; data: ParcelFormData }) => {
           const { data: response } = await api.put(`/parcels/${id}`, data)
           return response
         }
       - onSuccess: () => {
           queryClient.invalidateQueries({ queryKey: ['parcels'] })
           toast.success('Parcel updated successfully', {
             duration: 3000,
             icon: <CheckCircle className="h-4 w-4 text-green-500" />,
           })
         }
       - onError: (error: any) => {
           const message = error.response?.data?.message || error.message || 'Failed to update parcel'
           toast.error(message, {
             duration: 5000,
             icon: <XCircle className="h-4 w-4 text-red-500" />,
           })
         }

    Export function signature:
    ```typescript
    export function useUpdateParcel() {
      const queryClient = useQueryClient()
      return useMutation({ ... })
    }
    ```

    This hook will be used in ParcelSidebar edit mode onSubmit handler.
  </action>
  <verify>
    <automated>grep -q "export function useUpdateParcel" src/hooks/useUpdateParcel.ts && grep -q "api.put.*parcels" src/hooks/useUpdateParcel.ts && grep -q "invalidateQueries.*parcels" src/hooks/useUpdateParcel.ts && grep -q "Parcel updated successfully" src/hooks/useUpdateParcel.ts</automated>
  </verify>
  <done>
    useUpdateParcel.ts exists with useMutation hook, puts to /parcels/{id} endpoint, invalidates parcels query on success, shows success/error toast notifications
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useDeleteParcel mutation hook</name>
  <files>src/hooks/useDeleteParcel.ts</files>
  <read_first>
    - src/api/axios.ts
    - src/hooks/useUpdateParcel.ts
  </read_first>
  <action>
    Create src/hooks/useDeleteParcel.ts as a React Query mutation hook for deleting parcels.

    Implementation requirements:
    1. Import useMutation, useQueryClient from '@tanstack/react-query'
    2. Import api from '@/api/axios'
    3. Import toast from 'react-hot-toast'
    4. Import CheckCircle and XCircle from 'lucide-react'
    5. Use useQueryClient to get queryClient instance
    6. Return useMutation with:
       - mutationFn: async (id: number) => {
           await api.delete(`/parcels/${id}`)
         }
       - onSuccess: () => {
           queryClient.invalidateQueries({ queryKey: ['parcels'] })
           toast.success('Parcel deleted successfully', {
             duration: 3000,
             icon: <CheckCircle className="h-4 w-4 text-green-500" />,
           })
         }
       - onError: (error: any) => {
           const message = error.response?.data?.message || error.message || 'Failed to delete parcel'
           toast.error(message, {
             duration: 5000,
             icon: <XCircle className="h-4 w-4 text-red-500" />,
           })
         }

    Export function signature:
    ```typescript
    export function useDeleteParcel() {
      const queryClient = useQueryClient()
      return useMutation({ ... })
    }
    ```

    This hook will be used in DeleteConfirmModal and ParcelSidebar delete action.
  </action>
  <verify>
    <automated>grep -q "export function useDeleteParcel" src/hooks/useDeleteParcel.ts && grep -q "api.delete.*parcels" src/hooks/useDeleteParcel.ts && grep -q "invalidateQueries.*parcels" src/hooks/useDeleteParcel.ts && grep -q "Parcel deleted successfully" src/hooks/useDeleteParcel.ts</automated>
  </verify>
  <done>
    useDeleteParcel.ts exists with useMutation hook, deletes /parcels/{id} endpoint, invalidates parcels query on success, shows success/error toast notifications
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DeleteConfirmModal component</name>
  <files>src/components/map/DeleteConfirmModal.tsx</files>
  <read_first>
    - src/lib/utils.ts
  </read_first>
  <action>
    Create src/components/map/DeleteConfirmModal.tsx as a confirmation modal for delete actions.

    Props interface:
    ```typescript
    interface DeleteConfirmModalProps {
      isOpen: boolean
      onClose: () => void
      onConfirm: () => void
      isDeleting?: boolean
    }
    ```

    Implementation requirements per UI-SPEC.md:
    1. Import AlertTriangle from 'lucide-react'
    2. Modal overlay (fixed position, z-index 30):
       - Background: `bg-slate-900/50` (semi-transparent black)
       - Full screen: `fixed inset-0 flex items-center justify-center`
       - Animation: fade-in effect
    3. Modal container:
       - Background: white
       - Width: max-w-md (400px max)
       - Border radius: rounded-lg (8px)
       - Shadow: shadow-2xl
       - Padding: p-8 (32px)
    4. Modal content:
       - Icon: AlertTriangle in red-500, 48px size, centered at top
       - Heading: "Delete Parcel", text-slate-900, font-semibold, text-lg
       - Body text: "Are you sure you want to delete this parcel? This action cannot be undone.", text-slate-600, text-sm
       - Margin: spacing between elements (24px or 6)
    5. Modal actions:
       - Layout: flex gap-3 mt-6
       - Cancel button: flex-1 bg-slate-100 text-slate-700 hover:bg-slate-200 py-2 rounded-md
       - Delete button: flex-1 bg-red-500 text-white hover:bg-red-600 py-2 rounded-md, disabled when isDeleting
       - Delete button shows "Deleting..." when isDeleting is true

    Return null if isOpen is false.
    6. Add conditional rendering: `if (!isOpen) return null`
    7. Handle Escape key to close modal (optional but good UX)

    Styling summary from UI-SPEC:
    - Overlay: fixed inset-0 bg-slate-900/50 z-30
    - Container: bg-white rounded-lg shadow-2xl p-8 max-w-md w-full mx-4
    - Icon: h-12 w-12 text-red-500 mx-auto
    - Heading: text-lg font-semibold text-slate-900 text-center mt-4
    - Body: text-sm text-slate-600 text-center mt-2
    - Actions: flex gap-3 mt-6
  </action>
  <verify>
    <automated>grep -q "export function DeleteConfirmModal" src/components/map/DeleteConfirmModal.tsx && grep -q "AlertTriangle" src/components/map/DeleteConfirmModal.tsx && grep -q "text-red-500" src/components/map/DeleteConfirmModal.tsx && grep -q "bg-slate-900/50" src/components/map/DeleteConfirmModal.tsx</automated>
  </verify>
  <done>
    DeleteConfirmModal.tsx exists with overlay (bg-slate-900/50, z-30), container (white, rounded-lg, shadow-2xl, p-8), AlertTriangle icon in red-500, heading "Delete Parcel", warning body text, Cancel and Delete buttons
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Update/delete requests sent to backend |
| user→modal | User confirms destructive action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | useUpdateParcel | mitigate | Zod parcelSchema validates data before sending. Backend validates as well. Cache invalidation ensures fresh data after update. |
| T-03-06-02 | Tampering | useDeleteParcel | mitigate | Requires explicit user confirmation via modal. No bulk delete - single parcel only. Backend validates parcel exists and ownership (if auth added later). |
| T-03-06-03 | Spoofing | DeleteConfirmModal | accept | No CSRF protection in v1 (portfolio/demo). Backend should implement CSRF for production. |
| T-03-06-04 | Information Disclosure | Toast messages | accept | Success/error messages are non-sensitive. No credentials or data exposure. |
| T-03-06-05 | Elevation of Privilege | Update/Delete without auth | accept | No authentication in v1 (portfolio/demo). All users can edit/delete - intentional for demo. |
</threat_model>

<verification>
- useUpdateParcel puts to /parcels/{id} endpoint
- useDeleteParcel deletes /parcels/{id} endpoint
- Both hooks invalidate parcels query on success
- DeleteConfirmModal shows warning with AlertTriangle icon
- TypeScript compilation succeeds
</verification>

<success_criteria>
1. useUpdateParcel.ts exists with useMutation hook
2. useUpdateParcel posts to /parcels/{id} with PUT method
3. useUpdateParcel invalidates queries on success
4. useDeleteParcel.ts exists with useMutation hook
5. useDeleteParcel deletes /parcels/{id} with DELETE method
6. useDeleteParcel invalidates queries on success
7. DeleteConfirmModal.tsx exists with overlay and modal container
8. DeleteConfirmModal shows AlertTriangle icon in red-500
9. DeleteConfirmModal has "Delete Parcel" heading and warning message
10. DeleteConfirmModal has Cancel and Delete buttons
11. TypeScript compilation succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-crud-operations/03-06-SUMMARY.md`
</output>
